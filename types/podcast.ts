export type PodcastState='IDLE'|'GENERATING_OUTLINE'|'INTRO'|'NOVA_SPEAKING'|'KAEL_SPEAKING'|'BREAK_IN_CONNECTING'|'CALLER_LIVE'|'CALLER_TRANSCRIBING'|'HOSTS_RESPONDING'|'REDIRECT_PENDING'|'SEGMENT_TRANSITION'|'EPISODE_END'|'PAUSED';export type Host='NOVA'|'KAEL';export type TranscriptSpeaker='NOVA'|'KAEL'|'CALLER'|'SYSTEM';export type SegmentStatus='completed'|'in_progress'|'pending'|'user_added';export interface EpisodeSegment{segment:number;title:string;duration:string;status:SegmentStatus}export interface EpisodeOutline{episode_title:string;total_estimated_duration:string;outline:EpisodeSegment[]}export interface TranscriptAnnotation{id:string;status:'confirmed'|'disputed'|'needs_context';note?:string;createdAt:number}export interface TranscriptEntry{id:string;speaker:TranscriptSpeaker;text:string;isStreaming?:boolean;isCallIn?:boolean;timestamp:number;segmentIndex?:number;tldr?:string;annotation?:TranscriptAnnotation;sourceRef?:string}export type SourceType='pdf'|'url'|'text'|'youtube'|'csv';export interface PodcastSource{id:string;name:string;type:SourceType;content?:string;url?:string;citationCount:number;uploadedAt:number;embedded:boolean;chunkCount?:number}export type RedirectAction='GO_DEEPER'|'SPEED_UP'|'DEVILS_ADVOCATE'|'ELI5'|'BRING_DATA';export interface InterruptionConfig{sentenceBudget:number;willInterrupt:boolean}export interface CollaborativeSession{sessionId:string;hostUserId:string;callerQueue:CallerInQueue[];observers:Observer[]}export interface CallerInQueue{id:string;label:string;color:string;joinedAt:number}export interface Observer{id:string;joinedAt:number}